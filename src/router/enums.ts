export const enum RouterPath {
  Home = '/',
  Course = '/course/:courseOrder',
  Auth = '/auth',
  Profile = '/profile',
  Workout = '/workout',
  NotFound = '*',
  Sandbox = '/sandbox'
}

export const enum LinkPath {
  Course = '/course'
}